import fs from 'node:fs/promises';
import { chromium } from 'playwright';
const portal=process.env.CFB_PORTAL_URL||'https://cfb-betting-intelligence.netlify.app/';
const fpi=JSON.parse(await fs.readFile('data/espn-fpi-current.json','utf8')),board=JSON.parse(await fs.readFile('data/weekly-board.json','utf8')),failures=[];
if(!Array.isArray(fpi.teams)||fpi.teams.length<100)failures.push(`FPI coverage ${fpi.teams?.length||0} < 100`);if((fpi.teams||[]).filter(x=>Number.isFinite(Number(x.fpi))).length<100)failures.push('FPI numeric coverage < 100 teams');
const target=(board.games||[]).find(g=>g.currentSpread)||board.games?.[0]||null;
const browser=await chromium.launch({headless:true}),page=await browser.newPage({viewport:{width:1440,height:1100}}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(`${portal}?multiModel=${Date.now()}`,{waitUntil:'domcontentloaded',timeout:120000});await page.waitForFunction(()=>window.CFB_MULTI_MODEL?.state?.ready===true&&Number(window.CFB_WEEKLY_BOARD_2026?.week)===Number("+String(board.week)+"),{timeout:60000}).catch(()=>{});await page.waitForTimeout(1800);let out;
try{out=await Promise.race([page.evaluate(async targetId=>{const sleep=ms=>new Promise(r=>setTimeout(r,ms)),sel=document.getElementById('modelSel'),options=Array.from(sel?.options||[]).map(o=>o.value),games=typeof weeks!=='undefined'?(weeks[currentWeek]?.games||[]):[],x=games.find(g=>String(g.id)===String(targetId))||games.find(g=>/\d/.test(String(g.market||'')))||games[0];let independence=null;if(x){const before=window.CFB_MULTI_MODEL.calculate(x,'independent'),p=String(x.game||'').split(/ at | vs /),rt=(window.CFB_WEEKLY_BOARD_2026?.games||[]).find(g=>String(g.away)===p[0]?.trim()&&String(g.home)===p[1]?.trim());if(rt){const old=rt.currentSpread;rt.currentSpread=(p[1]?.trim()||'Home')+' -41.5';const after=window.CFB_MULTI_MODEL.calculate(x,'independent');rt.currentSpread=old;independence={before:before?.homeMargin,after:after?.homeMargin}}}if(sel){sel.value='independent';sel.dispatchEvent(new Event('change',{bubbles:true}));for(let i=0;i<20&&window.CFB_MULTI_MODEL?.state?.selected!=='independent';i++)await sleep(100);await sleep(400)}const independentTitle=document.getElementById('betsTitle')?.innerText||'',boardSelected=document.querySelector('#scheduleRows tr')?.dataset?.selectedModel||null,bestRows=document.querySelectorAll('#betsRows tr[data-model-candidate="independent"]').length;document.querySelector('nav button[data-tab="match"]')?.click();await sleep(150);const ms=document.getElementById('matchSel'),realOption=Array.from(ms?.options||[]).find(o=>o.value===x?.id)||Array.from(ms?.options||[]).find(o=>String(o.value||'').trim());if(ms&&realOption){ms.value=realOption.value;ms.dispatchEvent(new Event('change',{bubbles:true}));window.CFB_MULTI_MODEL?.refresh?.();window.CFB_MATCHUP_CENTER_V2?.render?.();await sleep(1000)}const panel=document.getElementById('multiModelMatchupPanel'),visible=document.querySelector('#matchupCenterV2 .mc2-models'),text=`${visible?.textContent||''} ${panel?.textContent||''}`;return{options,independence,selectedState:window.CFB_MULTI_MODEL?.state?.selected||null,independentTitle,boardSelected,bestRows,matchupValue:ms?.value||null,panel:!!visible,sourcePanel:!!panel,text,fpiUpdatedAt:window.CFB_MULTI_MODEL?.state?.fpi?.updatedAt||null,week:window.CFB_WEEKLY_BOARD_2026?.week||null}},target?.gameId||null),new Promise((_,reject)=>setTimeout(()=>reject(new Error('Multi-model browser evaluation exceeded 45 seconds')),45000))])}catch(error){failures.push(`browser verification timeout/error: ${error.message}`);out={options:[],independence:null,independentTitle:'',boardSelected:null,bestRows:0,panel:false,sourcePanel:false,text:'',fpiUpdatedAt:null}}finally{await browser.close().catch(()=>{})}
for(const m of ['ensemble','independent','market'])if(!out.options.includes(m))failures.push(`model selector missing ${m}`);if(Number(out.week)!==Number(board.week))failures.push(`portal selected week=${out.week}, expected ${board.week}`);if(!out.independence||!Number.isFinite(out.independence.before)||Math.abs(out.independence.before-out.independence.after)>.000001)failures.push(`Football Independent fair changed when market was mutated: ${JSON.stringify(out.independence)}`);if(!/Football Independent/.test(out.independentTitle))failures.push('Best Bets did not switch to Football Independent');if(out.boardSelected!=='independent')failures.push(`Weekly Board selected model=${out.boardSelected}`);if(!out.panel)failures.push('Matchup Center multi-model panel missing');for(const t of ['Football Independent','Market-Calibrated','Ensemble','ESPN FPI','higher is better'])if(!out.text.includes(t))failures.push(`Matchup panel missing ${t}`);for(const e of errors)failures.push(`pageerror: ${e}`);const report={checkedAt:new Date().toISOString(),week:out.week,fpiUpdatedAt:out.fpiUpdatedAt,selector:out.options,selectedState:out.selectedState,matchupValue:out.matchupValue,sourcePanel:out.sourcePanel??false,independence:out.independence,bestRows:out.bestRows,status:failures.length?'FAIL':'PASS',failures};await fs.writeFile('data/multi-model-verification-current.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(failures.length)throw new Error(`MULTI MODEL VERIFICATION FAIL: ${failures.join(' | ')}`);console.log('CFB MULTI MODEL VERIFICATION PASS');await import('./verify-statistical-display.mjs');await import('./verify-market-derived-integrity.mjs');