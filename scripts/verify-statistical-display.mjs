import fs from 'node:fs/promises';
import { chromium } from 'playwright';
const portal=process.env.CFB_PORTAL_URL||'https://cfb-betting-intelligence.netlify.app/';
const board=JSON.parse(await fs.readFile('data/weekly-board.json','utf8'));
const metrics=JSON.parse(await fs.readFile('data/team-metrics-current.json','utf8'));
const fpi=JSON.parse(await fs.readFile('data/espn-fpi-current.json','utf8'));
const season=Number(board.season||2026),week=Number(board.week),expectedTeams=new Set((board.games||[]).flatMap(g=>[g.away,g.home]).filter(Boolean)).size;
const failures=[];
if(Number(metrics.week)!==week)failures.push(`team metrics week ${metrics.week} != ${week}`);
if((metrics.coverage?.resolvedTeams||0)!==expectedTeams)failures.push(`team metrics coverage ${metrics.coverage?.resolvedTeams||0}/${expectedTeams}`);
if((metrics.coverage?.unresolvedTeams||[]).length)failures.push(`unresolved stat teams: ${metrics.coverage.unresolvedTeams.join(', ')}`);
if((fpi.teams||[]).filter(x=>Number.isFinite(Number(x.fpi))).length<100)failures.push('ESPN FPI numeric coverage < 100 teams');
const updated=Date.parse(metrics.updatedAt||''),fpiUpdated=Date.parse(fpi.updatedAt||'');if(!Number.isFinite(updated)||Date.now()-updated>6*3600000)failures.push('team metrics artifact stale >6h');if(!Number.isFinite(fpiUpdated)||Date.now()-fpiUpdated>6*3600000)failures.push('ESPN FPI artifact stale >6h');
const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1440,height:1100}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(`${portal}?statIntegrity=${Date.now()}`,{waitUntil:'domcontentloaded',timeout:120000});await page.waitForFunction(()=>window.CFB_RUNTIME_DATA?.status==='remote-active'&&window.CFB_MULTI_MODEL?.state?.ready===true,{timeout:60000});await page.waitForTimeout(1000);
const live=await page.evaluate(async()=>{const sleep=ms=>new Promise(r=>setTimeout(r,ms));document.querySelector('nav button[data-tab="match"]')?.click();await sleep(100);const sel=document.getElementById('matchSel');let checked=0,statsWithValues=0,fpiPanels=0;for(let i=0;i<Math.min(sel?.options?.length||0,12);i++){sel.selectedIndex=i;sel.dispatchEvent(new Event('change',{bubbles:true}));if(typeof window.renderMatch==='function')window.renderMatch(sel.value);await sleep(80);const p=document.getElementById('cfbMatchupStatsPanel'),m=document.getElementById('multiModelMatchupPanel');if(p){checked++;const t=p.innerText||'';if(/Yds\/play|Success|Explosive|PPG|YPG/i.test(t)&&/\d/.test(t))statsWithValues++;}if(m&&/ESPN FPI/.test(m.innerText||''))fpiPanels++;}return{options:sel?.options?.length||0,checked,statsWithValues,fpiPanels};});await browser.close();
if(live.options!==Number(board.scheduleCoverage?.slateCount||board.games?.length))failures.push(`matchup selector ${live.options} != expected slate`);if(live.checked<Math.min(10,live.options))failures.push(`statistical panel rendered only ${live.checked} sampled matchups`);if(live.statsWithValues<Math.min(8,live.checked))failures.push(`too few sampled matchup panels expose numeric stats: ${live.statsWithValues}/${live.checked}`);if(live.fpiPanels<Math.min(8,live.checked))failures.push(`too few sampled matchup panels expose ESPN FPI context: ${live.fpiPanels}/${live.checked}`);for(const e of errors)failures.push(`browser pageerror: ${e}`);
const report={season,week,verifiedAt:new Date().toISOString(),status:failures.length?'FAIL':'PASS',detail:`Team/stat coverage ${metrics.coverage?.resolvedTeams||0}/${expectedTeams}; ESPN FPI ${(fpi.teams||[]).filter(x=>Number.isFinite(Number(x.fpi))).length} numeric teams; sampled rendered statistical panels ${live.statsWithValues}/${live.checked}; FPI panels ${live.fpiPanels}/${live.checked}.`,coverage:{expectedTeams,resolvedTeams:metrics.coverage?.resolvedTeams||0,fpiNumeric:(fpi.teams||[]).filter(x=>Number.isFinite(Number(x.fpi))).length,...live},failures};await fs.writeFile(`data/statistical-display-integrity-${season}-w${week}.json`,JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(failures.length)throw new Error(`STATISTICAL DISPLAY INTEGRITY FAIL: ${failures.join(' | ')}`);console.log('CFB STATISTICAL DISPLAY INTEGRITY PASS');
